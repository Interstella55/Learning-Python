# Import random below:
import random

# This code will make the system select a random number from 1 to 100. You can change the numbers. 
random_list = []
random_list = [random.randint(1, 100) for i in range(101)]
randomer_number = random.choice(random_list)
print(randomer_number)
